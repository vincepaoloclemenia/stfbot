<% if user_signed_in? && current_user.role == 'company_admin' %>
    <%= render 'company_admin' %>
<% elsif user_signed_in? && current_user.role == 'finance admin' %>
    <%= render 'finance_admin' %>
<% end %>